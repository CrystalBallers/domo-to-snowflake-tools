# Testing dependencies for Domo to Snowflake Migration Tools
# Install with: pip install -r requirements-test.txt
#
# Only the packages actually exercised by the suite are listed. Install the
# runtime deps too (pip install -r requirements.txt) before running tests.

# Core testing framework
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-mock>=3.10.0
pytest-timeout>=2.1.0
pytest-xdist>=3.0.0      # enables run_tests.py --parallel (-n auto)

# Data libraries used directly inside tests
pandas>=2.0.0
polars>=0.20.0

# Coverage reporting
coverage>=7.0.0

# Building the offline inventory template (tools/scripts/make_inventory_template.py)
openpyxl>=3.1.0
